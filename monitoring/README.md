# Monitoring

See the [independent monitoring platform](platform/README.md) for Prometheus,
Grafana, Pushover alerts and private Tailscale collection between hosts.

The original `install-monitoring.zsh` manages the separate legacy installation.
